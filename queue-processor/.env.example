# Required: Supabase REST API configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional but recommended: Direct database access for cross-schema operations
# Uncomment and set this if you need to access tables in non-public schemas
# DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# Optional Configuration
# BATCH_SIZE=20                # Default: 20 records per batch
# DELAY_BETWEEN_BATCHES=5000   # Default: 5000ms (5 seconds) between batches
# MAX_RETRIES=3                # Default: 3 retries per batch 