require("dotenv").config(); const { Pool } = require("pg"); async function testDB() { try { const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const result = await pool.query("SELECT 1 AS test"); console.log("✅ Database connection successful:", result.rows); await pool.end(); } catch (err) { console.error("❌ Database connection failed:", err.message); } } testDB();
