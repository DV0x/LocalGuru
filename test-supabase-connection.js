require("dotenv").config(); const { createClient } = require("@supabase/supabase-js"); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false } }); async function testConnection() { try { const { data, error } = await supabase.from("content_chunks").select("id").limit(1); console.log("Connection successful:", !error, data ? "Data retrieved" : "No data"); if (error) console.error("Error:", error); } catch (e) { console.error("Exception:", e.message); } } testConnection();
